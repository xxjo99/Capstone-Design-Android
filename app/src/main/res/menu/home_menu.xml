<?xml version="1.0" encoding="utf-8"?> <!-- 홈화면 하단바 -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 카테고리 -->
    <item
        android:id="@+id/menu_home_home"
        android:enabled="true"
        android:icon="@drawable/home_icon"
        android:title="@string/home"
        app:showAsAction="always" />

    <!-- 등록글 리스트 -->
    <item
        android:id="@+id/menu_home_post"
        android:enabled="true"
        android:icon="@drawable/post_icon"
        android:title="@string/post"
        app:showAsAction="always" />

    <!-- 나의 참가 / 등록글 -->
    <item
        android:id="@+id/menu_home_myPost"
        android:enabled="true"
        android:icon="@drawable/mypost_icon"
        android:title="@string/myPost"
        app:showAsAction="always" />

    <!-- 검색 -->
    <item
        android:id="@+id/menu_home_search"
        android:enabled="true"
        android:icon="@drawable/search_icon"
        android:title="@string/find"
        app:showAsAction="always" />

    <!-- 내정보 -->
    <item
        android:id="@+id/menu_home_myPage"
        android:enabled="true"
        android:icon="@drawable/mypage_icon"
        android:title="@string/myInfo"
        app:showAsAction="always" />

</menu>